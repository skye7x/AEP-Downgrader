<UserControl x:Class="AEPDowngrader.Controls.InputDropZone"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             AllowDrop="True"
             MinHeight="90"
             Cursor="Hand"
             d:DesignHeight="90" d:DesignWidth="500">
    <Border x:Name="RootBorder"
            BorderThickness="1"
            BorderBrush="{StaticResource BorderBrush2}"
            Background="{StaticResource PanelBrush}"
            CornerRadius="8">
        <Border.Resources>
            <Style TargetType="Border">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsEnabled}" Value="False">
                        <Setter Property="Background" Value="{StaticResource DropZoneDisabledBackgroundBrush}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource DropZoneDisabledBorderBrush}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Resources>
        <StackPanel Margin="12,10" VerticalAlignment="Center">
            <TextBlock x:Name="TitleLabel"
                       Text="Drop .aep files here or click to browse"
                       FontSize="14" FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"/>
            <TextBlock x:Name="DetailsLabel"
                       Text="No files selected"
                       Margin="0,6,0,0"
                       Foreground="{StaticResource TextBrush}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</UserControl>
