<Window x:Class="AEPDowngrader.Views.UpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Available"
        Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        ResizeMode="NoResize">
    <StackPanel Margin="20">
        <TextBlock x:Name="TitleText" FontWeight="Bold" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,10"/>
        <TextBlock x:Name="InfoText" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,10"/>
        <Expander x:Name="NotesExpander" Header="Release notes" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10" Visibility="Collapsed">
            <TextBox x:Name="NotesTextBox" IsReadOnly="True" TextWrapping="Wrap" MaxHeight="200"
                     VerticalScrollBarVisibility="Auto" Style="{StaticResource DarkTextBoxStyle}" Margin="0,8,0,0"/>
        </Expander>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Remind Later" Style="{StaticResource RegularButtonStyle}" Margin="0,0,8,0" Padding="12,6" Click="RemindLater_Click"/>
            <Button Content="Skip this Version" Style="{StaticResource RegularButtonStyle}" Margin="0,0,8,0" Padding="12,6" Click="SkipVersion_Click"/>
            <Button Content="Download" Style="{StaticResource PrimaryButtonStyle}" Padding="16,6" Click="Download_Click" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
